# Codex 구현 계획서

## 1. 문서 목적

이 문서는 `wlan-troubleshooter-ko`의 구현 범위와 완료 기준을 고정합니다. 현재 실행 지시는 Phase 0과 Phase 1만 대상으로 하며, Phase 2는 명시적으로 중단 상태입니다.

이 계획은 한 번에 전체 분석기를 만드는 대신 데이터 반출 방지와 결정론적 안전 기반을 먼저 검증하기 위한 것입니다.

## 2. 제품 정의

제품 목표는 다음 한 문장으로 정의합니다.

> Aruba WLAN, ClearPass, 사용자 VLAN, 무선 Air Capture에서 수집한 로컬 PCAP·PCAPNG를 외부 전송 없이 분석해, 단말의 접속 절차 중 확인 가능한 단계와 판단 한계를 패킷 근거와 함께 한국어로 안내하는 Windows 11 Portable 도구

프로그램은 최종적으로 다음 질문에 답해야 합니다.

1. 어느 단계까지 정상으로 확인되는가?
2. 어느 단계에서 명시적인 실패가 관찰되는가?
3. 판단을 뒷받침하는 프레임과 필터는 무엇인가?
4. 현재 캡처로 판단할 수 없는 영역은 무엇인가?
5. 초급 엔지니어가 다음으로 확인할 위치는 어디인가?

## 3. 변경할 수 없는 제품 원칙

- 제품은 완전 오프라인으로 동작합니다.
- AI와 자유 생성형 설명을 사용하지 않습니다.
- 분석은 버전이 고정된 JSON 규칙과 사전 검수한 한국어 문장으로 수행합니다.
- 증상 선택은 결과 우선순위만 바꾸며 핵심 단계 검사를 임의로 생략하지 않습니다.
- 패킷 부재를 실패 증거로 사용하지 않습니다.
- 확인 가능한 사실, 가능한 원인, 판단 불가를 분리합니다.
- 같은 입력과 같은 도구·규칙 버전에서는 같은 정규화 결과를 생성합니다.
- 외부 업로드, 텔레메트리, 자동 업데이트, 온라인 조회를 포함하지 않습니다.
- 장비 접속, 설정 조회·변경, 실시간 캡처를 포함하지 않습니다.
- Raw Payload와 자격 증명 추출 기능을 포함하지 않습니다.

## 4. 현재 범위와 단계 경계

### Phase 0: 저장소 기반과 데이터 반출 방지

목표는 이후 기능이 안전한 경계 밖으로 확장되지 않도록 저장소 규칙과 자동 감사를 먼저 세우는 것입니다.

구현 대상:

- 프로젝트 설명, 작업 규칙, 구현 계획, 동적 상태 문서
- AI·외부 통신 금지 ADR
- 승인된 Portable TShark ADR
- 결정론적 JSON 규칙 ADR
- Python 3.13 프로젝트 골격과 런타임 의존성 0개 선언
- 실제 PCAP, 사이트 데이터, 로그, 보고서, 벤더 바이너리 커밋 방지 규칙
- 네트워크·AI Import, 외부 URL, 위험한 실행 구문 정적 감사
- 합성 데이터만 사용하는 테스트 정책
- Windows CI 기본 작업

완료 기준:

- 새 작업자가 문서만 읽고 허용·금지 범위를 구분할 수 있습니다.
- 저장소 감사가 추적된 실제 캡처와 금지 산출물을 실패 처리합니다.
- 소스 감사가 금지 Import, 외부 URL, `eval`, `exec`, `shell=True`를 탐지합니다.
- Windows CI가 Python 3.13에서 컴파일, 테스트, 감사, 자체 점검을 실행합니다.
- 제3자 코드·바이너리·실제 사내 데이터가 없습니다.

### Phase 1: 최소 애플리케이션과 안전한 실행 경계

목표는 실제 분석 규칙에 들어가기 전에 로컬 파일 처리와 실패 동작을 검증하는 것입니다.

구현 대상:

- `tkinter/ttk` 기반 최소 애플리케이션 창
- 오프라인 상태와 현재 미구현 범위를 명확히 보여 주는 한국어 문구
- PCAP·PCAPNG 파일 선택
- 정규 파일, 허용 확장자, 형식 식별 바이트, 최소 길이 기초 검증
- 원본을 수정하지 않는 읽기 전용 처리
- 성공·예외·취소 시 정리되는 임시 작업공간
- 민감 문자열과 식별자를 마스킹하는 로컬 로그 경계
- 버전이 고정된 JSON 리소스 로더와 정규화 직렬화
- Portable TShark 매니페스트와 SHA-256 검증
- 허용된 실행 파일과 인자만 만드는 TShark 정책 계층
- 외부 참조와 JavaScript를 거부하는 HTML 검사기
- 창을 띄우지 않고 실행할 수 있는 `--self-test`

완료 기준:

- GUI 모듈을 가져오는 것만으로 Tk 루트나 네트워크 연결이 만들어지지 않습니다.
- 잘못된 확장자, 잘못된 식별 바이트, 잘린 파일을 안전하게 거부합니다.
- 테스트가 만든 임시 파일은 정상·오류·취소 경로에서 모두 정리됩니다.
- 로그에 Payload, 자격 증명, 사용자 ID 원문, 전체 IP·MAC, 절대경로가 남지 않습니다.
- 같은 합성 입력을 반복 처리한 정규화 JSON이 바이트 단위로 같습니다.
- Portable TShark가 없으면 `미준비`, 매니페스트나 해시가 다르면 `무결성 오류`로 실패합니다.
- 임의 옵션, 실시간·원격 캡처, Lua 실행, 이름 해석이 실행 정책을 통과하지 못합니다.
- HTML 검사기는 외부 리소스, 스크립트, 프레임, 자동 새로고침, CSS 외부 참조를 거부합니다.
- macOS 로컬 테스트와 Windows CI 결과를 구분해 기록합니다.

### Phase 2: 현재 시작 금지

다음 항목은 사용자의 명시적인 새 지시 전에는 구현하지 않습니다.

- TShark를 통한 실제 프로토콜 필드 추출
- 캡처 유형과 분석 가능 범위 판별
- EAP, RADIUS, DHCP, DNS, ARP, TCP, TLS 이벤트 생성
- 단말별 세션과 접속 상태 머신
- 실제 장애 판정 규칙과 한국어 Finding
- 완성형 단일 HTML 보고서
- 802.11, 4-Way Handshake, Radiotap, 로밍 분석
- PyInstaller 패키징과 공개 릴리스

Phase 0·1의 모든 자동 검증이 통과하고 미검증 범위가 명확히 기록된 뒤에만 Phase 2 진입을 검토할 수 있습니다.

## 5. 목표 저장소 구조

```text
wlan-troubleshooter-ko/
├─ .github/workflows/
│  └─ windows-ci.yml
├─ docs/adr/
│  ├─ 0001-no-ai-and-no-network.md
│  ├─ 0002-approved-portable-tshark.md
│  └─ 0003-deterministic-json-rules.md
├─ scripts/
│  ├─ audit_repository.py
│  ├─ audit_source.py
│  └─ verify_offline.ps1
├─ src/wlan_troubleshooter_ko/
│  ├─ __init__.py
│  ├─ __main__.py
│  ├─ app.py
│  ├─ core/
│  │  ├─ canonical_json.py
│  │  ├─ capture.py
│  │  ├─ config.py
│  │  ├─ logging_policy.py
│  │  ├─ redaction.py
│  │  └─ workspace.py
│  ├─ report/
│  │  └─ offline_html.py
│  ├─ tshark/
│  │  ├─ manifest.py
│  │  ├─ policy.py
│  │  ├─ runner.py
│  │  └─ status.py
│  ├─ resources/
│  │  ├─ messages/ko.json
│  │  ├─ profiles/site-default.example.json
│  │  └─ rules/v1/rules.json
│  └─ ui/
│     └─ main_window.py
├─ tests/
├─ vendor/wireshark/
│  ├─ README.md
│  └─ manifest.example.json
├─ AGENTS.md
├─ CODEX_First_Execution_Prompt.txt
├─ CODEX_IMPLEMENTATION_PLAN.md
├─ IMPLEMENTATION_STATUS.md
├─ LICENSE
├─ README.md
├─ THIRD_PARTY_NOTICES.md
└─ pyproject.toml
```

실제 파일명은 구현 과정에서 더 단순하게 조정할 수 있지만 보안 경계와 모듈 책임을 합치면 안 됩니다.

## 6. 최소 UI 동작

Phase 1 화면은 다음 요소만 갖습니다.

- 제품명과 `완전 오프라인` 표시
- `PCAP 또는 PCAPNG 선택` 버튼
- 선택한 파일의 안전한 표시명과 기초 검증 결과
- Portable TShark의 준비 상태
- 현재 단계에서는 실제 분석이 지원되지 않는다는 설명
- 종료 동작

사용자 ID, IP, MAC, 증상, 시간 범위 입력과 분석 결과 화면은 Phase 2 이후입니다. Phase 1에서 성공한 분석 결과를 모방한 예시 카드를 표시하지 않습니다.

## 7. 입력 파일 검증

PCAP은 신뢰할 수 없는 입력입니다.

- `.pcap`, `.pcapng` 확장자만 허용합니다.
- 확장자와 실제 식별 바이트가 일치해야 합니다.
- 알려진 PCAP 바이트 순서 형식과 PCAPNG Section Header Block만 허용합니다.
- 비정규 파일과 명백히 잘린 헤더를 거부합니다.
- 파일 크기와 경로 관련 오류는 민감한 절대경로를 노출하지 않는 한국어 오류로 변환합니다.
- 원본 파일을 열 때 쓰기 모드를 사용하지 않습니다.
- 검증 성공은 캡처 전체가 안전하거나 분석 가능하다는 보증이 아닙니다.

Phase 1 테스트는 테스트 실행 중 임시 파일에 최소 합성 헤더를 만들며 바이너리 fixture를 커밋하지 않습니다.

## 8. 임시 작업공간과 로그

임시 작업공간은 컨텍스트 관리 방식으로 소유권을 명확히 합니다.

- 운영체제가 제공하는 임시 디렉터리 아래에 예측하기 어려운 이름으로 생성합니다.
- 작업공간 밖으로 벗어나는 상대경로와 심볼릭 링크를 거부합니다.
- 성공, 예외, 사용자 취소에서 같은 정리 경로를 사용합니다.
- 원본 캡처를 임시공간 정리 대상으로 포함하지 않습니다.

로그는 운영 진단 이벤트만 남깁니다.

- Raw Payload와 TShark 원문 출력을 기록하지 않습니다.
- 쿠키, Authorization, 비밀번호, 토큰을 완전히 제거합니다.
- 사용자 ID, IP, MAC, 파일 경로는 마스킹하거나 안정적인 비가역 식별자로 바꿉니다.
- 로그 마스킹 실패 시 원문을 기록하는 대신 해당 필드를 버립니다.

## 9. 결정론적 JSON 기반

리소스에는 `schema_version`과 자체 콘텐츠 버전을 둡니다.

- 규칙 ID는 저장소 전체에서 유일해야 합니다.
- 중복 ID, 알 수 없는 필드, 잘못된 형식은 시작 시 실패합니다.
- JSON 객체 키와 결과 목록의 정렬 기준을 고정합니다.
- 시간은 캡처 안의 시간만 사용하고 현재 시각을 분석 결과에 넣지 않습니다.
- 기간은 부동소수점 대신 고정 단위 정수를 우선합니다.
- 한국어 문자열은 일관된 유니코드 정규화를 적용합니다.
- 결과 직렬화는 UTF-8, 고정 separators, 고정 줄바꿈을 사용합니다.

Phase 1의 규칙 파일은 구조와 로딩을 검증하기 위한 빈 골격 또는 비진단 메타데이터만 포함합니다. 실제 WLAN 장애 규칙을 추가하지 않습니다.

## 10. 오프라인 정적 감사

감사 대상은 적어도 런타임 소스와 패키지 리소스입니다.

차단 범주:

- `socket`, `ssl`, `urllib`, `http`, `ftplib` 등 네트워크 표준 모듈
- `requests`, `aiohttp`, `websockets` 등 네트워크 패키지
- `openai`, `anthropic`, `ollama`, `langchain` 등 AI 관련 패키지
- 외부 통신 스킴 문자열
- `eval`, `exec`, `compile`의 동적 실행 용도
- `subprocess`의 문자열 명령, `shell=True`
- 수신 대기 서버와 자동 업데이트 흔적

문서의 출처 표기와 개발용 Git 주소는 런타임 소스 감사 범위와 구분합니다. 예외 목록이 필요하면 코드에 숨기지 않고 이유와 적용 경로를 명시한 데이터로 관리합니다.

## 11. HTML 정책 검사

Phase 0·1에서는 보고서를 만들지 않고 안전 정책 검사기를 준비합니다.

검사기는 다음을 거부합니다.

- 모든 JavaScript와 `script`
- 네트워크 스킴과 프로토콜 상대 URL
- 외부 `src`, `href`, `action`
- `base`, `iframe`, `object`, `embed`
- 자동 새로고침
- CSS `url()`과 `@import`

허용 가능한 최종 형태는 인라인 CSS, 텍스트, 필요한 경우 검증된 `data:` 이미지로 구성된 단일 HTML입니다. SVG는 현재 허용 목록에 포함하지 않습니다. 최종 CSP는 기본 차단을 원칙으로 합니다.

## 12. 테스트 계획

### 저장소와 소스 감사

- 실제 PCAP 확장자와 PCAP 식별 바이트 탐지
- 사내 프로파일·로그·보고서·추출물 금지 경로 탐지
- 금지 Import와 외부 URL 탐지
- 위험한 프로세스 실행과 동적 코드 실행 탐지
- 정상 소스에 대한 오탐 방지

### 입력 검증

- 지원하는 PCAP 바이트 순서별 최소 헤더
- PCAPNG 최소 Section Header Block
- 확장자 불일치
- 식별 바이트 불일치
- 잘린 파일과 빈 파일
- 비정규 파일과 읽기 오류

### 작업공간과 마스킹

- 정상 종료 후 정리
- 예외 후 정리
- 취소 후 정리
- 경로 이탈 차단
- IP, MAC, 사용자 ID, Authorization, 쿠키, 토큰 마스킹
- 마스킹할 수 없는 값의 폐기

### 결정론

- 객체 입력 순서가 달라도 같은 바이트 결과
- 반복 실행 결과 일치
- 중복 규칙 ID와 잘못된 스키마 거부
- 유니코드 정규화 결과 일치

### TShark 정책

- 매니페스트 없음, 파일 없음, 해시 불일치
- 승인된 합성 실행 파일의 해시 일치
- 고정 인자 순서와 `-n` 포함
- 실시간·원격·Lua·임의 옵션 거부
- 실제 캡처를 열지 않는 고정 `-n -v` 준비 확인에만 제한시간, 취소·종료, `shell=False` 적용
- `AnalysisWorkspace.allocate()`가 반환한 미존재 하위 경로에만 빈 격리 트리를 배타 생성하고 실행 직전 재검증
- 필드 추출은 실행하지 않고 승인 호출 정보만 준비

실제 캡처 필드 추출 실행은 Phase 2에서 구현합니다.

### HTML 정책

- 인라인 CSS만 있는 최소 문서 허용
- 외부 링크, 이미지, 폼, 스크립트 거부
- CSS URL과 import 거부
- 프레임, 객체, 자동 새로고침 거부

### UI와 자체 점검

- Tk 루트 없이 UI 모듈 Import
- ViewModel 상태 전이
- 잘못된 파일의 쉬운 한국어 안내
- TShark 미준비·무결성 오류 구분
- `--self-test`가 창을 열거나 네트워크를 사용하지 않고 종료

## 13. Windows CI

새 저장소에는 Windows 검증 작업을 추가합니다.

- `runs-on: windows-latest`
- Python 3.13
- 최소 권한 `contents: read`
- 고정 제한시간
- 바이트코드 컴파일
- 전체 단위 테스트
- 소스·저장소 감사
- 비대화형 자체 점검
- PowerShell 오프라인 정책 검사

GitHub-hosted 실행 결과는 Windows 코드 호환성 근거입니다. 다음 항목은 별도 실제 검증 전까지 미검증으로 남깁니다.

- 실제 데스크톱에서의 GUI 상호작용
- 승인된 Portable TShark와 DLL 전체
- 네트워크 어댑터 비활성 상태
- Windows 방화벽 아웃바운드 차단 상태
- 실제 사내 캡처와 실제 장비 조건
- PyInstaller Portable ZIP

## 14. 제3자와 라이선스

- 프로젝트 코드는 MIT 라이선스로 새로 작성합니다.
- 비교한 저장소의 코드를 복사하거나 변형해 가져오지 않습니다.
- GPL 프로젝트에서는 일반적인 아이디어만 참고하며 코드·자산을 가져오지 않습니다.
- TShark 바이너리는 승인 전 소스 저장소에 넣지 않습니다.
- 향후 실제 배포본에 TShark가 포함되면 정확한 버전, 파일 해시, 저작권 고지, 라이선스 원문, 소스 제공 관련 의무를 검토해 배포물에 반영합니다.
- 제3자 구성요소를 추가하기 전에 `THIRD_PARTY_NOTICES.md`를 갱신합니다.

## 15. 구현 결과 보고 형식

작업 완료 보고에는 다음을 포함합니다.

1. 변경한 파일
2. 구현한 Phase 0·1 항목
3. 실행한 명령과 실제 통과·실패 결과
4. Windows CI 작업과 결과
5. 승인 TShark 부재 등 차단 사항
6. 실제 장비·GUI·방화벽·패키징 등 미검증 범위
7. Phase 2를 시작하지 않았다는 확인

테스트 실패, 감사 실패, 바이너리 부재를 숨기거나 완료로 바꾸어 표현하지 않습니다.

## 16. Phase 2 진입 조건

다음 조건을 모두 충족하고 사용자가 명시적으로 승인해야 합니다.

- Phase 0·1 문서와 코드가 일치합니다.
- 로컬 테스트와 Windows CI가 통과합니다.
- 실제 PCAP과 민감 데이터가 저장소에 없습니다.
- 네트워크·AI 정적 감사가 통과합니다.
- TShark 공급·라이선스·해시 관리 방식이 승인되었습니다.
- 남은 실제 Windows 검증 범위를 사용자가 확인했습니다.

현재 지시에서는 Phase 2를 시작하지 않습니다.
