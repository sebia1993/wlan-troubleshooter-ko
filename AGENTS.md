# 저장소 작업 규칙

이 파일은 저장소 전체에 적용됩니다. 작업자는 변경 전에 다음 파일을 순서대로 읽습니다.

1. `AGENTS.md`
2. `CODEX_IMPLEMENTATION_PLAN.md`
3. `IMPLEMENTATION_STATUS.md`
4. `docs/adr/`의 관련 결정 기록

명시적인 최신 사용자 지시가 우선하며, 범위를 넓히는 해석은 허용하지 않습니다.

## 현재 허용 범위

- Phase 0과 Phase 1만 구현합니다.
- Phase 2의 프로토콜 이벤트 추출, 장애 분석 규칙, 실제 HTML 보고서 생성, 패키징을 시작하지 않습니다.
- 사용자가 별도로 Phase 2 시작을 명시하기 전에는 분석 기능이 완성된 것처럼 표시하지 않습니다.
- 동작하지 않는 기능을 임시 성공값, 가짜 Finding, 예제 결과로 숨기지 않습니다.

## 기술 기준

- Python 3.13을 사용합니다.
- 런타임 코드는 Python 표준 라이브러리만 사용합니다.
- 데스크톱 UI는 `tkinter/ttk`로 작성합니다.
- UI, 입력 검증, TShark 경계, 규칙, 보고서, 보안 감사를 분리합니다.
- 플랫폼과 무관한 로직은 Tk 루트를 만들지 않고 테스트할 수 있어야 합니다.
- 공개 함수와 보안 경계에는 타입 힌트와 짧고 정확한 설명을 둡니다.

## 완전 오프라인 원칙

제품 런타임에는 다음 요소를 추가하지 않습니다.

- AI, LLM, Ollama, MCP, 모델 SDK
- 외부 API, 웹 요청, 소켓 연결, DNS 조회
- 텔레메트리, 오류 자동 전송, 사용량 수집
- 자동 업데이트와 온라인 버전 확인
- 수신 대기 포트, HTTP 서버, 원격 제어
- API 키, 토큰, URL을 입력하거나 저장하는 설정

런타임 소스에서 네트워크 및 AI 관련 Import, 외부 URL 문자열, `eval`, `exec`, `shell=True`를 정적 감사로 차단합니다. Git·GitHub Actions 같은 개발 인프라 통신은 제품 런타임 통신과 구분하되, 애플리케이션이나 빌드 스크립트가 실행 구성요소를 임의로 내려받게 해서는 안 됩니다.

## TShark 실행 경계

- 저장소가 지정한 `vendor/wireshark/` 아래의 승인된 Portable TShark만 후보로 삼습니다.
- 시스템 설치 경로, 레지스트리, `PATH`에서 발견한 실행 파일로 자동 대체하지 않습니다.
- 버전과 모든 필수 파일의 SHA-256이 승인 매니페스트와 일치해야 합니다.
- 매니페스트 누락, 파일 누락, 해시 불일치는 안전한 실패로 처리합니다.
- 자동 다운로드나 자동 교체를 구현하지 않습니다.
- Phase 1의 유일한 `subprocess` 실행은 고정 `-n -v` 준비 확인이며, 인자 배열, `shell=False`, 제한시간·취소·종료 처리를 강제합니다.
- 저장 파일 입력에는 항상 이름 해석 비활성화 옵션 `-n`을 강제합니다.
- 실시간 인터페이스, 원격 캡처, `rpcap`, `TCP@호스트:포트`, 임의 extcap, 임의 Lua, 임의 사용자 옵션을 허용하지 않습니다.
- 사용자 Wireshark 설정과 플러그인을 자동으로 불러오지 않도록 승인 버전별 격리 방식을 검증합니다.
- Phase 1에서는 실행 정책, 무결성 검사, 준비 상태와 비캡처 버전 확인까지만 구현합니다. 실제 패킷 이벤트 추출은 Phase 2입니다.

## 데이터 보호

- PCAP과 PCAPNG는 확장자만 믿지 않고 파일 형식 식별 바이트와 기본 구조를 검사합니다.
- 심볼릭 링크, 비정규 파일, 작업공간 밖 경로를 신뢰하지 않습니다.
- 원본 캡처는 수정하거나 자동 삭제하지 않습니다.
- 임시 작업공간은 운영체제 임시 디렉터리에 만들고 정상 종료·예외·취소 시 정리합니다.
- 로그에는 Raw Payload, 쿠키, Authorization 값, 비밀번호, 인증서 비밀값, 사용자 ID 원문, 전체 IP·MAC, 원본 파일의 절대경로를 기록하지 않습니다.
- 캡처 내용이나 TShark 표준출력을 무심코 예외 메시지에 포함하지 않습니다.
- 저장소에 실제 사내 캡처, 사이트 프로파일, 장비 설정, 사용자 정보, 로그, 보고서, 추출 파일을 커밋하지 않습니다.
- 테스트는 실행 중 생성하는 최소 합성 바이트와 합성 JSON을 사용합니다.
- `.gitignore` 외에도 추적 파일과 파일 식별 바이트를 확인하는 저장소 감사를 유지합니다.

## 결정론과 판정 안전성

- 규칙과 한국어 메시지는 버전이 있는 JSON으로 관리합니다.
- 규칙은 스키마 검증 후 고정된 순서로 평가합니다.
- 현재 시각, 난수, 절대경로, 운영체제 로캘, 순회 순서가 분석 결과를 바꾸지 않게 합니다.
- 정규화 결과는 키, 목록, 근거 프레임, Finding의 정렬 기준을 고정합니다.
- 결론과 관찰 사실을 분리하고 모든 결론에 근거 프레임과 Display Filter를 연결합니다.
- 패킷 부재를 장애 발생으로 해석하지 않습니다.
- TCP Retransmission만으로 RF 장애를 확정하지 않습니다.
- Radiotap이 없으면 RSSI·SNR·채널·무선 Retry를 판단하지 않습니다.
- RADIUS 패킷이 없으면 ClearPass 장애를 확정하지 않습니다.
- 짧거나 단방향인 캡처에서 서버 무응답을 확정하지 않습니다.
- 근거가 부족하거나 서로 충돌하면 `판단 불가`가 우선합니다.

## HTML 안전 기준

향후 보고서는 외부 파일이 없는 단일 HTML이어야 합니다. 다음 요소를 허용하지 않습니다.

- JavaScript와 모든 `script` 요소
- 외부 `src`, `href`, `action`
- `base`, `iframe`, `object`, 자동 새로고침
- CSS `url()`과 `@import`
- HTTP, HTTPS, WebSocket 참조

Phase 0·1에서는 보고서 생성기가 아니라 이 정책을 확인하는 검사기와 테스트까지만 다룹니다.

## 검증과 보고

변경 후 최소한 다음 항목을 실행하고 실제 결과를 보고합니다.

- Python 바이트코드 컴파일
- 전체 단위 테스트
- 애플리케이션 비대화형 자체 점검
- 네트워크·AI·위험 구문 정적 감사
- 실제 PCAP·민감 산출물 커밋 방지 감사
- Windows GitHub Actions

macOS 로컬 테스트를 Windows 검증으로 표현하지 않습니다. GitHub-hosted Windows 작업은 일반 코드·테스트 호환성 근거이며, 실제 Tk 창 조작, 승인 TShark/DLL, 네트워크 어댑터 비활성화, 방화벽 차단, 실제 캡처 분석을 검증한 것으로 표현하지 않습니다.

`IMPLEMENTATION_STATUS.md`에는 구현됨, 자동 검증됨, 미검증, 차단됨을 구분합니다. 통과하지 않은 항목을 완료로 표시하지 않습니다.

## 변경과 출처

- 비교한 외부 저장소의 소스, 테스트, 문장, 이미지, 자산을 복사하지 않습니다.
- 외부 프로젝트에서는 기능 개념과 UX 원칙만 참고해 독립 구현합니다.
- 제3자 코드나 바이너리가 실제로 추가되면 출처, 버전, 커밋, 라이선스, 포함 파일을 `THIRD_PARTY_NOTICES.md`에 먼저 기록합니다.
- 의도한 파일만 스테이징하고 무관한 작업자의 변경을 덮어쓰지 않습니다.
- 완료 보고에는 변경 파일, 실행한 검증, 실제 결과, 남은 제한사항을 포함합니다.
