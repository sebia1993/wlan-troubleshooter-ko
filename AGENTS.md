# 저장소 작업 규칙

이 파일은 저장소 전체에 적용됩니다. 작업자는 변경 전에 다음 파일을 순서대로 읽습니다.

1. `AGENTS.md`
2. `CODEX_IMPLEMENTATION_PLAN.md`
3. `docs/PHASE_2A_PLAN.md`
4. `IMPLEMENTATION_STATUS.md`
5. `docs/adr/`의 관련 결정 기록

명시적인 최신 사용자 지시가 우선하며 범위를 넓히는 해석은 허용하지 않습니다.

## 현재 허용 범위

사용자는 2026년 9월 3일 Phase 0·1 이후 개발과 릴리즈 갱신을 명시적으로 승인했습니다. 현재 허용 범위는 `docs/PHASE_2A_PLAN.md`의 **캡처 구조 사전 점검**과 검증된 소스 프리뷰 릴리즈입니다.

- PCAP·PCAPNG 구조, Link Type, Snap Length, 패킷 잘림과 분석 가능 범위를 구현합니다.
- 실제 프로토콜 필드 추출, 장애 규칙, 완성형 보고서, 패킷 판정은 다음 단계로 남깁니다.
- 동작하지 않는 기능을 임시 성공값, 가짜 Finding, 예제 결과로 숨기지 않습니다.
- Phase 2A 결과를 실제 WLAN 장애 분석이 완료된 것처럼 표시하지 않습니다.

## 기술 기준

- Python 3.13을 사용합니다.
- 런타임 코드는 Python 표준 라이브러리만 사용합니다.
- 데스크톱 UI는 `tkinter/ttk`로 작성합니다.
- UI, 입력 검증, TShark 경계, 분석, 규칙, 보고서, 보안 감사를 분리합니다.
- 플랫폼과 무관한 로직은 Tk 루트를 만들지 않고 테스트할 수 있어야 합니다.
- 공개 함수와 보안 경계에는 타입 힌트와 짧고 정확한 설명을 둡니다.

## 완전 오프라인 원칙

제품 런타임에는 AI, LLM, Ollama, MCP, 모델 SDK, 외부 API, 웹 요청, 소켓 연결, DNS 조회, 텔레메트리, 자동 업데이트, 온라인 버전 확인, 수신 대기 포트, HTTP 서버, 원격 제어, API 키·토큰·URL 설정을 추가하지 않습니다.

런타임 소스에서 네트워크 및 AI 관련 Import, 외부 URL 문자열, `eval`, `exec`, `shell=True`를 정적 감사로 차단합니다. Git·GitHub Actions·GitHub Release 같은 개발 인프라 통신은 제품 런타임 통신과 구분하되 애플리케이션이나 빌드 스크립트가 실행 구성요소를 내려받아서는 안 됩니다.

## TShark 실행 경계

- `vendor/wireshark/` 아래의 승인된 Portable TShark만 후보로 사용합니다.
- 시스템 설치 경로, 레지스트리, `PATH` 실행 파일로 자동 대체하지 않습니다.
- 버전과 필수 파일의 SHA-256이 승인 매니페스트와 일치해야 합니다.
- 매니페스트·파일 누락과 해시 불일치는 안전한 실패로 처리합니다.
- 자동 다운로드와 자동 교체를 구현하지 않습니다.
- 모든 저장 캡처 분석 명령에 이름 해석 비활성화 `-n`을 강제합니다.
- 실시간·원격 캡처, rpcap, TCP@호스트:포트, 임의 extcap, 임의 Lua, 임의 사용자 옵션을 허용하지 않습니다.
- Phase 2A에서는 TShark로 캡처를 열지 않습니다. 실제 필드 추출은 Phase 2B에서 별도 승인 범위로 구현합니다.

## 데이터 보호

- PCAP·PCAPNG는 확장자와 식별 바이트·기본 구조를 함께 검사합니다.
- 심볼릭 링크, 비정규 파일, 작업공간 밖 경로를 신뢰하지 않습니다.
- 원본 캡처는 수정하거나 자동 삭제하지 않습니다.
- 임시 작업공간은 정상·예외·취소 시 정리합니다.
- 로그에는 Raw Payload, 자격 증명, 파일명 원문, 전체 IP·MAC, 원본 절대경로를 기록하지 않습니다.
- 캡처 내용이나 TShark 원문을 예외 메시지에 포함하지 않습니다.
- 실제 사내 캡처, 사이트 프로파일, 사용자 정보, 장비 설정, 로그, 보고서, 추출 파일을 커밋하지 않습니다.
- 테스트는 실행 중 생성하는 최소 합성 바이트와 합성 JSON만 사용합니다.

## Phase 2A 구조 점검 안전성

- PCAP 레코드와 PCAPNG 블록 수에 상한을 둡니다.
- 파일 경계를 넘는 선언 길이와 비정상적으로 큰 레코드·블록을 거부합니다.
- PCAPNG 블록 시작·종료 길이가 다르면 실패합니다.
- 사용자 취소를 레코드·블록 경계에서 확인합니다.
- 검사 전후 파일의 형식·크기·SHA-256이 다르면 실패합니다.
- 제한에 도달하면 전체 검사가 완료된 것처럼 표시하지 않습니다.
- PPI Link Type은 내부 캡슐화를 확인하기 전 802.11·RF·IP 캡처로 확정하지 않습니다.
- Link Type은 실제 프로토콜의 존재 증거가 아니라는 점을 UI에 표시합니다.

## 결정론과 판정 안전성

- 규칙과 한국어 메시지는 버전이 있는 JSON으로 관리합니다.
- 현재 시각, 난수, 절대경로, 운영체제 로캘, 외부 조회 결과를 결과에 반영하지 않습니다.
- 정규화 결과의 키와 목록 정렬 기준을 고정합니다.
- 향후 모든 결론에 근거 프레임과 Display Filter를 연결합니다.
- 패킷 부재를 장애 발생으로 해석하지 않습니다.
- TCP Retransmission만으로 RF 장애를 확정하지 않습니다.
- Radiotap이 없으면 RSSI·SNR·채널·무선 Retry를 판단하지 않습니다.
- RADIUS 패킷이 없으면 ClearPass 장애를 확정하지 않습니다.
- 짧거나 단방향인 캡처에서 서버 무응답을 확정하지 않습니다.
- 근거가 부족하면 `판단 불가`가 우선입니다.

## HTML 안전 기준

향후 보고서는 외부 파일이 없는 단일 HTML이어야 합니다. JavaScript, 외부 `src`·`href`·`action`, `base`, `iframe`, `object`, 자동 새로고침, CSS `url()`·`@import`, HTTP·WebSocket 참조를 허용하지 않습니다.

## 릴리즈 기준

- `v0.2.0-alpha.1`은 Python 3.13이 필요한 소스 프리뷰입니다.
- 승인 TShark, Python 런타임, DLL 또는 제3자 실행 파일을 자동 다운로드하거나 릴리즈 자산에 포함하지 않습니다.
- 릴리즈 워크플로는 전체 컴파일·테스트·소스 감사·저장소 감사·자체 점검이 통과한 뒤에만 GitHub Release를 생성하거나 갱신합니다.
- 프리뷰 릴리즈를 설치형 Windows 완성판으로 표현하지 않습니다.

## 검증과 보고

변경 후 Python 바이트코드 컴파일, 전체 단위 테스트, 비대화형 자체 점검, 네트워크·AI·위험 구문 감사, 실제 PCAP·민감 산출물 커밋 방지 감사, Windows GitHub Actions를 실행합니다.

macOS·Linux 검증을 Windows 검증으로 표현하지 않습니다. GitHub-hosted Windows 작업도 승인 TShark, 네트워크 어댑터 비활성화, 방화벽 차단 상태, 실제 캡처 분석을 검증한 것으로 표현하지 않습니다.

`IMPLEMENTATION_STATUS.md`에는 구현됨, 자동 검증됨, 미검증, 차단됨을 구분합니다.

## 변경과 출처

- 외부 저장소의 소스, 테스트, 문장, 이미지, 자산을 복사하지 않습니다.
- 외부 프로젝트에서는 기능 개념과 UX 원칙만 참고해 독립 구현합니다.
- 제3자 코드나 바이너리가 추가되면 출처, 버전, 라이선스, 커밋, 포함 파일을 `THIRD_PARTY_NOTICES.md`에 먼저 기록합니다.
- 의도한 파일만 변경하고 무관한 작업을 덮어쓰지 않습니다.
- 완료 보고에는 변경 파일, 실행한 검증, 실제 결과, 남은 제한사항을 포함합니다.
