# 저장소 작업 규칙

변경 전에 이 문서, CODEX_IMPLEMENTATION_PLAN.md, 현재 Phase 계획, IMPLEMENTATION_STATUS.md와 관련 ADR을 읽습니다. 초기 계획의 Phase 0/1 한정 문구는 당시 범위이며, 사용자가 이후 명시적으로 승인한 계속 개발 지시에 따라 현재 Phase를 진행합니다.

## 현재 범위

`docs/PHASE_4L_PLAN.md`: 상대 시간·거래 경계의 검증·배포 마무리. 이어서 기존 오프라인 HTML 정책 검사기를 사용하는 한국어 요약 보고서를 별도 검증합니다. 사용자는 매 작업 종료 시 남은 작업과 근거가 명시된 전체 진행률을 보고하도록 요청했습니다.

## 변경 불가 원칙

- 제품은 Python 3.13 표준 라이브러리와 tkinter/ttk 및 배포된 고정 TShark만 사용합니다. 사용자 PC에 Python·Wireshark 별도 설치를 요구하지 않습니다.
- AI·LLM·Ollama·MCP·외부 API·HTTP·소켓·온라인 DNS·업로드·텔레메트리·오류 전송·자동 업데이트·수신 서버·원격 제어·실시간 캡처를 추가하지 않습니다.
- 실제 사내 캡처·장비 설정·사용자 정보·분석 결과를 저장소·Actions·외부 도구에 업로드하지 않습니다. 검증은 실행 중 만드는 합성 데이터만 사용합니다.
- 네트워크·AI import, 외부 URI, 동적 실행, shell=True와 무승인 프로세스 실행을 감사로 차단합니다. 개발 공급망 다운로드는 제품 런타임과 분리합니다.
- 원본은 읽기 전용입니다. 입력 경로·형식·크기·SHA-256을 단계 전후 확인하고 링크·Junction·Reparse Point·UNC 경로를 거부합니다.
- vendor/wireshark의 고정 매니페스트만 사용합니다. 시스템 설치본·PATH 대체 실행을 금지하고 실행 전후 파일 크기·SHA-256을 확인합니다.
- shell=False, stdin 비활성화, -n, 저장 파일 -r, 패킷 상한 -c, 승인된 fields만 허용합니다. Lua·extcap·임의 필터·원문 stderr를 허용하지 않습니다.
- 빈 config/plugin/extcap/data/temp 격리 공간, 출력·시간 상한, 취소·오류 시 정리를 유지합니다.
- raw MAC/IP/SSID/BSSID, 사용자명·DNS 질의명·포트, 원본 거래 ID, 절대 epoch, 파일명·경로, HMAC 키·토큰, Replay Counter 원문, Nonce/MIC/Key Data/Payload를 공개 결과에 추가하지 않습니다.
- DEVICE-N/AP-N은 한 분석 실행 안에서만 유효합니다. 기존 가명을 원문 없이 재추측하거나 시간 근접성·AP 목록 첫 값·SSID로 연결하지 않습니다.
- 패킷 부재·드롭 0·ISB 부재·일반적인 EAPOL 관계로 정상·실패·무손실·키 설치·근본 원인을 확정하지 않습니다.

## PCAPNG 통계 경계

섹션별 엔디언, 블록 앞뒤 길이·정렬·옵션 경계·동일 옵션 중복·인터페이스 선언을 검증합니다. 허용 Counter는 ifrecv/ifdrop/filteraccept/osdrop/usrdeliv뿐입니다. 여러 ISB를 합산하지 않으며 Counter 변화와 보고값만 제공합니다. 문자열 옵션·절대 시각은 공개하지 않습니다.

## 시간 경계

전용 필드는 frame.number와 frame.time_epoch뿐입니다. 상대 밀리초만 직렬화하고 원문 epoch는 보관하지 않습니다. 역행 탐지는 표시 반올림 전에 수행합니다. 거래 이벤트 시간 범위와 양 끝 프레임 차이는 구분하며, 불완전 근거로 정확한 검증 완료를 주장하지 않습니다.

## 보고서 경계

단일 HTML은 기존 validate_offline_html 검사를 통과해야 합니다. 외부 CSS/JS/이미지/폰트·폼·자동 브라우저 실행을 추가하지 않습니다. 기존 결과의 승인된 필드만 사용하고, 임의 메타데이터 문자열을 보고서에 복사하지 않습니다. 저장은 사용자 동작으로만 수행하며 기존 파일을 덮어쓰지 않습니다.

## 검증·보고

동일 최신 HEAD의 Windows CI와 Portable 실제 합성 분석을 통과한 뒤 병합합니다. 게시 후 실제 ZIP·SHA-256·태그·커밋을 확인합니다. 미검증 또는 실패를 성공으로 표시하지 않습니다. 코드 구현, CI 통과, 병합, 릴리스 게시, 현장 검증을 구분합니다. 전체 진행률은 고정 체크리스트와 가중치로 산정하며 코드 줄 수·단계 이름·테스트 개수를 진척률로 쓰지 않습니다.
