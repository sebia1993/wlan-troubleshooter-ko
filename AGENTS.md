# 저장소 작업 규칙

이 파일은 저장소 전체에 적용됩니다. 변경 전에 `AGENTS.md`, `CODEX_IMPLEMENTATION_PLAN.md`, 현재 Phase 계획, `IMPLEMENTATION_STATUS.md`, 관련 ADR을 순서대로 읽습니다. 명시적인 최신 사용자 지시가 우선하며 범위를 넓히는 해석은 허용하지 않습니다.

## 현재 허용 범위

사용자는 2026년 9월 3일 Phase 2A 이후 개발과 릴리즈 갱신을 승인했습니다. 현재 범위는 `docs/PHASE_2B_PLAN.md`의 **TShark 필드 호환성·프로토콜 인벤토리 기반**과 검증된 소스 프리뷰입니다.

- 필드 카탈로그·고정 프로파일·TSV 파서·프로토콜 존재 정규화·실행 전 argv 준비를 구현합니다.
- 승인 TShark가 없으므로 실제 `-G fields` 및 캡처 `-T fields` 실행은 활성화하지 않습니다.
- EAP·RADIUS·DHCP·DNS·TCP 이벤트와 장애 Finding은 다음 단계로 남깁니다.
- 동작하지 않는 기능을 가짜 결과나 임시 성공값으로 숨기지 않습니다.

## 기술과 완전 오프라인 원칙

- Python 3.13 표준 라이브러리와 `tkinter/ttk`만 런타임에 사용합니다.
- AI, LLM, Ollama, MCP, 외부 API, 웹 요청, 소켓, DNS 조회, 텔레메트리, 자동 업데이트, 온라인 버전 확인, 수신 포트, HTTP 서버, 원격 제어, API 키·토큰·URL 설정을 추가하지 않습니다.
- 네트워크·AI Import, 외부 URL, `eval`, `exec`, `shell=True`를 정적 감사로 차단합니다.
- GitHub Actions와 Release 같은 개발 인프라 통신은 제품 런타임과 구분하되 앱과 빌드 스크립트가 실행 구성요소를 내려받아서는 안 됩니다.

## TShark 경계

- `vendor/wireshark/` 아래 승인된 Portable TShark만 후보로 사용합니다.
- 시스템 설치본, 레지스트리, `PATH` 실행 파일로 자동 대체하지 않습니다.
- 매니페스트, 버전, 파일 크기와 SHA-256이 일치해야 합니다.
- 누락·해시 불일치는 안전한 실패로 처리합니다.
- 모든 명령에 `-n`을 강제하며 실시간·원격 캡처, rpcap, TCP@호스트, 임의 extcap·Lua·사용자 옵션을 허용하지 않습니다.
- Phase 2B의 프로토콜 프로파일에는 `frame.number`, 선택적인 `frame.interface_id`, `frame.cap_len`, `frame.len`, `frame.protocols`만 허용합니다.
- IP·MAC·SSID·사용자명·Payload 필드는 승인하지 않습니다.

## 데이터 보호와 판정 안전성

- 입력은 확장자·매직·구조·정규 파일·링크 여부를 함께 검사합니다.
- 원본 캡처는 수정하거나 자동 삭제하지 않습니다.
- 임시 작업공간은 정상·예외·취소 시 정리합니다.
- 로그와 GUI에 Raw Payload, 자격 증명, 파일명 원문, 전체 IP·MAC, 절대경로, TShark 표준 오류 원문을 남기지 않습니다.
- 실제 사내 캡처·사이트 프로파일·사용자 정보·장비 설정·로그·보고서를 커밋하지 않습니다.
- 테스트는 런타임 생성 합성 PCAP·PCAPNG·JSON·TSV만 사용합니다.
- 프로토콜 존재는 성공·실패를 의미하지 않으며 미관찰도 장애 증거가 아닙니다.
- 패킷 부재를 장애로 해석하지 않습니다.
- TCP 재전송만으로 RF 장애를 확정하지 않습니다.
- Radiotap이 없으면 RSSI·SNR·채널·무선 Retry를 판단하지 않습니다.
- RADIUS 패킷이 없으면 ClearPass 장애를 확정하지 않습니다.
- 근거가 부족하면 `판단 불가`가 우선입니다.

## 검증·릴리즈

변경 후 바이트코드 컴파일, 전체 단위 테스트, 비대화형 자체 점검, 소스 감사, 저장소 감사를 Windows CI에서 실행합니다. macOS·Linux 검증이나 합성 테스트를 승인 TShark·실제 사내 캡처 검증으로 표현하지 않습니다.

`v0.2.0-alpha.2`는 Python 3.13 소스 프리뷰입니다. 승인 TShark·Python 런타임·DLL·설치 프로그램을 자동 다운로드하거나 자산에 포함하지 않습니다. 전체 검증이 통과한 뒤에만 Release를 생성합니다.

외부 프로젝트의 코드를 복사하지 않으며 제3자 코드나 바이너리가 추가되면 출처·버전·라이선스·포함 파일을 `THIRD_PARTY_NOTICES.md`에 먼저 기록합니다.
